---
- name: "Test Vagrant base VMs creation"
  hosts: hypervisor
  gather_facts: no
  tasks:
    - name: "Check Base VMs"
      ansible.builtin.import_role:
        name: ../../roles/host_hypervisor_management
        tasks_from: check_base_vms.yml